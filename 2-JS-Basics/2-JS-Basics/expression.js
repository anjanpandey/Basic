// Statements and Expressions

//statement (performs an action doesnot produces an immediate value)
function someFun(par) {
    //code
}

//expression (produces a value or an outcome)
var someFun = function (par) {
    //code
}

//Expression 
3 + 4;
var x = 3;

/*
In above expressions a result is produced immediately. One reuslt is stored in variable x.
*/


// Statements

if (x === 5) {
    //do something 
}

/*
In above statement, an action is performed and it leads to the result.
However, an immediate result is not produced. 
*/
